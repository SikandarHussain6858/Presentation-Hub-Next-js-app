export const metadata = {
    title: 'Dashboard',
    description: 'Manage your uploaded presentations and view your unique access codes.',
    robots: {
        index: false,
        follow: false,
    },
};

export default function DashboardLayout({ children }) {
    return children;
}
