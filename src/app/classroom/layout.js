export const metadata = {
    title: 'Classroom Mode',
    description: 'Enter your unique 6-digit code to instantly access and present your slides in the classroom.',
    keywords: ['classroom', 'present', 'code', 'access'],
    alternates: {
        canonical: '/classroom',
    },
};

export default function ClassroomLayout({ children }) {
    return children;
}
