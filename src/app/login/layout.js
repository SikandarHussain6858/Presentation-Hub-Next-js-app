export const metadata = {
    title: 'Login',
    description: 'Access your Presentation Hub account to manage your slides and presentations.',
    alternates: {
        canonical: '/login',
    },
};

export default function LoginLayout({ children }) {
    return children;
}
