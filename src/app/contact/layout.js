export const metadata = {
    title: 'Contact Us',
    description: 'Get in touch with the Presentation Hub team for support or inquiries.',
    alternates: {
        canonical: '/contact',
    },
};

export default function ContactLayout({ children }) {
    return children;
}
